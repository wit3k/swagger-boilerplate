required:
  - id
  - audit
  - groups
properties:
  id:
    type: string
    description: tags *name* and *id* is the same entity
  audit:
    $ref: "#/definitions/Audit"
  groups:
    type: array
    items:
      type: string
      format: uuid
