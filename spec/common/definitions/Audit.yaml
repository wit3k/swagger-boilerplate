required:
  - created
  - owner
properties:
  created:
    type: string
    format: date-time
  edited:
    type: string
    format: date-time
  archived:
    type: string
    format: date-time
  owner:
    type: string
    format: uuid
  editor:
    type: string
    format: uuid
  archiver:
    type: string
    format: uuid
